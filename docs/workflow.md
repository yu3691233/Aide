# 多 IDE 协同工作流

> 当前已改为 `main` 单分支轮流协作。旧的按 IDE 分支长期并行方案不再作为当前流程。

## 当前流程

1. 确认没有其他 IDE 正在改同一文件。
2. 从 `main` 拉取最新代码。
3. 完成一个可验证的逻辑单元。
4. 验证通过后提交到 `main`。
5. 如发现其他 IDE 留下完整但未提交的改动，先验证，再提交，不让半成品长期漂在工作区。

## 参考

更完整说明见 [`git-workflow.md`](git-workflow.md)。
