"# Mobile Chat Bridge & Assistant Delegation Rules\n\n每当在对话框中收到以 `[快捷回复]`、`[派发任务]` 或 `[任务反馈 ...]` 开头的输入时，请严格遵守以下操作协议：\n1. 立即读取本地绝对路径文件 C:/Users/mi/bridge/phone_in.txt，以获取用户的真实指令。\n2. 在当前打开的工作区/项目中执行该指令（如修改代码、运行命令等）。\n3. 执行完成后，将您的最终回复以 JSON 格式追加写入本地绝对路径文件 C:/Users/mi/bridge/chat_history.json（保持原 JSON 数组结构，添加 {\"sender\": \"agent\", \"text\": \"您的回复内容\", \"time\": \"当前时间\"}）。\n4. 在 IDE 聊天框中正常输出回复。\n\n## 【Token 省流与任务委派规则】\n当您（主力AI）需要执行以下简单或文本处理密集型任务时，优先通过执行后台命令，将任务委派给本地小助理（MiniMax-M3）执行，以节省您的 Token 消耗：\n\n### 1. 简单文本/文档/分析类任务：\n- 给代码编写单元测试、测试用例；\n- 编写代码注释、开发文档、API 文档；\n- 格式化文件、做总结、分析大量日志或翻译文本；\n- 解释某段代码的含义或进行基本的语法解释。\n\n**委派命令格式**：\n```bash\npython C:/Users/mi/bridge/call_assistant.py \"您的具体任务请求\"\n```\n\n### 2. 代码生成与重构类任务：\n- 编写独立的 Python 脚本、算法或复杂代码块；\n- 对现有代码进行重构、挑刺、Bug 修复。\n\n**协同命令格式**：\n```bash\npython C:/Users/mi/bridge/call_co_workers.py \"您的具体代码编写需求\"\n```\n小助理团队会在后台自动完成“编写 -> 审查与挑刺 -> 修复重构”的闭环，并直接输出高质量代码。获取小助理的返回结果后，再由您在本地整合并进行最终处理。\n"