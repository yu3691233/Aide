# ⚠️ 必读：多 IDE 协同规则

## 改代码前
1. `git pull origin main` 拉取最新
2. 告诉用户你要改哪个文件
3. 改完立即 `git commit && git push`

## 工作方式
- 所有 IDE 都在 main 分支上工作
- 一次只有一个 IDE 改代码，其他 IDE 只做分析和计划
- 改完就 push，下一个 IDE pull 后再开始

## 禁止
- ❌ 不改别人正在改的文件
- ❌ 不删别人加的功能
- ❌ 不覆盖别人的 import
- ❌ 不用 dev-* 分支

---

# Mobile Chat Bridge & Assistant Delegation Rules

每当在对话框中收到以 [快捷回复]、[派发任务] 或 [任务反馈 ...] 开头的输入时，请严格遵守以下操作协议：
1. 立即读取本地绝对路径文件 f:/AideLink/server/phone_in.txt，以获取用户的真实指令。
2. 在当前打开的工作区/项目中执行该指令（如修改代码、运行命令等）。
3. 执行完成后，将您的最终回复以 JSON 格式追加写入本地绝对路径文件 f:/AideLink/server/chat_history.json（保持原 JSON 数组结构，添加 {"sender": "agent", "text": "您的回复内容", "time": "当前时间"}）。
4. 在 IDE 聊天框中正常输出回复。

## 【Token 省流与任务委派规则】
当您（主力AI）需要执行以下简单或文本处理密集型任务时，优先通过执行后台命令，将任务委派给本地小梦灵（MiniMax-M3）执行，以节省您的 Token 消耗：

### 1. 简单文本/文档/分析类任务：
- 给代码编写单元测试、测试用例；
- 编写代码注释、开发文档、API 文档；
- 格式化文件、做总结、分析大量日志或翻译文本；
- 解释某段代码的含义或进行基本的语法解释。

**委派命令格式**：
```bash
python f:/AideLink/server/call_assistant.py "您的具体任务请求"
```

### 2. 代码生成与重构类任务：
- 编写独立的 Python 脚本、算法或复杂代码块；
- 对现有代码进行重构、挑刺、Bug 修复。

**协同命令格式**：
```bash
python f:/AideLink/server/call_co_workers.py "您的具体代码编写需求"
```
小梦灵团队会在后台自动完成“编写 -> 审查与挑刺 -> 修复重构”的闭环，并直接输出高质量代码。获取小梦灵的返回结果后，再由您在本地整合并进行最终处理。
